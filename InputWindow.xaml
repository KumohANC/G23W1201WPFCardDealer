<Window x:Class="G23W1201WPFCardDealer.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="InputWindow" Height="80" Width="210"
        FocusManager.FocusedElement="{Binding ElementName=Count}">
    <Grid Margin="5,0,5,0" Height="80" Width="200">
        <TextBox MaxLines="1" AcceptsReturn="False" x:Name="Count" Margin="0,0,95,56" KeyDown="Count_KeyDown">10000</TextBox>
        <Button Content="OK" Click="OKClick" Margin="112,0,20,56"/>
    </Grid>
</Window>
